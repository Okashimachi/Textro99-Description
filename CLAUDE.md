# CLAUDE.md — textro99-description

このリポジトリの説明・設計方針は **[AGENTS.md](./AGENTS.md)** に一本化している。

Claude Code は、作業を始める前に必ず **AGENTS.md を読むこと**。内容はそちらが正典。

> このプロジェクトでは複数のAIコーディングツール（Claude Code / Codex / Cursor 等）を使う可能性があるため、ツール非依存の `AGENTS.md` にルール本体を置き、`CLAUDE.md` は参照専用にしている。内容を更新する時は AGENTS.md 側を編集する（この CLAUDE.md は編集しない）。
